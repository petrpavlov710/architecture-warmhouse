# Project_template

Это шаблон для решения проектной работы. Структура этого файла повторяет структуру заданий. Заполняйте его по мере работы над решением.

# Задание 1. Анализ и планирование

### 1. Описание функциональности монолитного приложения

**Управление отоплением:**

- Пользователи могут удалённо включать/выключать отопление в своих домах.
- Система поддерживает прямые запросы от сервера.
- Невозмоможно задать желаемое значение температуры.

**Мониторинг температуры:**

- Пользователи могут просматривать текущую температуру в своих домах через веб-интерфейс.
- Система поддерживает прямые запросы от сервера.
- Информация с датчиков не используется для управление отоплением.

### 2. Анализ архитектуры монолитного приложения

- Язык программирования: Go
- База данных: PostgreSQL
- Архитектура: Монолитная, все компоненты системы (обработка запросов, бизнес-логика, работа с данными) находятся в рамках одного приложения.
- Взаимодействие: Синхронное, запросы обрабатываются последовательно.
- Масштабируемость: Ограничена, так как монолит сложно масштабировать по частям.
- Развертывание: Требует остановки всего приложения. Осуществляется с помощью Docker.

### 3. Определение доменов и границы контекстов

- **Домен: Управеление системой отопления**
  - поддомен управление отопителем
    - контекст: включение/выключение отопителя
  - поддомен мониторинга температуры
    - контекст: считывание данных с датчиков
	- контекст: предоставление пользователям данных с датчиков

### **4. Проблемы монолитного решения**

- Более затратное масштабирование в связи с будущим кратным увеличением числа пользователей
- Добавление новой кодовой базы в проект, для обеспечения нужного функционала, повлечет увеличение рисков ошибок и затрат времени на разработку и тестирование
- При внесении изменений (даже незначительных) в проект, приходится остановить и перезапустить все приложение. Такие простои в работе формируют негативное отношение пользователей

### 5. Визуализация контекста системы — диаграмма С4

```markdown
[Диаграмма контекста текущего монолитного приложения](docs/diagrams/as-is/C4_Context.puml)
```

# Задание 2. Проектирование микросервисной архитектуры

В этом задании вам нужно предоставить только диаграммы в модели C4. Мы не просим вас отдельно описывать получившиеся микросервисы и то, как вы определили взаимодействия между компонентами To-Be системы. Если вы правильно подготовите диаграммы C4, они и так это покажут.

**Диаграмма контейнеров (Containers)**

```markdown
[Диаграмма контейнеров будущего приложения](docs/diagrams/to-be/C4_Container.puml)
```

**Диаграмма компонентов (Components)**

```markdown
[Диаграмма компонентов микросервиса 'Управление пользователями'](docs/diagrams/to-be/C4_Component_Users.puml)
```

```markdown
[Диаграмма компонентов микросервиса 'Управление устройствами'](docs/diagrams/to-be/C4_Component_Control.puml)
```

```markdown
[Диаграмма компонентов микросервиса 'Мониторинг устройств'](docs/diagrams/to-be/C4_Component_Monitoring.puml)
```

**Диаграмма кода (Code)**

```markdown
[Диаграмма кода микросервиса 'Мониторинг устройств'](docs/diagrams/to-be/C4_Code_Monitoring.puml)
```

# Задание 3. Разработка ER-диаграммы

```markdown
[ER-диаграмма приложения](docs/diagrams/to-be/ER.puml)
```

# Задание 4. Создание и документирование API

### 1. Тип API

REST API. Так как это самый известный и простой тип.

### 2. Документация API

```markdown
[Документация Swagger](docs/swagger.yaml)
```

# Задание 5. Работа с docker и docker-compose

Перейдите в apps.

Там находится приложение-монолит для работы с датчиками температуры. В README.md описано как запустить решение.

Вам нужно:

1) сделать простое приложение temperature-api на любом удобном для вас языке программирования, которое при запросе /temperature?location= будет отдавать рандомное значение температуры.

Locations - название комнаты, sensorId - идентификатор названия комнаты

```
	// If no location is provided, use a default based on sensor ID
	if location == "" {
		switch sensorID {
		case "1":
			location = "Living Room"
		case "2":
			location = "Bedroom"
		case "3":
			location = "Kitchen"
		default:
			location = "Unknown"
		}
	}

	// If no sensor ID is provided, generate one based on location
	if sensorID == "" {
		switch location {
		case "Living Room":
			sensorID = "1"
		case "Bedroom":
			sensorID = "2"
		case "Kitchen":
			sensorID = "3"
		default:
			sensorID = "0"
		}
	}
```

2) Приложение следует упаковать в Docker и добавить в docker-compose. Порт по умолчанию должен быть 8081

3) Кроме того для smart_home приложения требуется база данных - добавьте в docker-compose файл настройки для запуска postgres с указанием скрипта инициализации ./smart_home/init.sql

Для проверки можно использовать Postman коллекцию smarthome-api.postman_collection.json и вызвать:

- Create Sensor
- Get All Sensors

Должно при каждом вызове отображаться разное значение температуры

Ревьюер будет проверять точно так же.
